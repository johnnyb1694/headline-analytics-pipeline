services:

  db:
    image: postgres
    container_name: newton-db
    restart: always
    user: postgres
    secrets:
      - db-name
      - db-password
    volumes:
      - db-data:/var/lib/postgresql/data
      - ./src/db/init.sql:/docker-entrypoint-initdb.d/init.sql
    environment:
      - POSTGRES_DB=/run/secrets/db-name
      - POSTGRES_PASSWORD_FILE=/run/secrets/db-password
    expose:
      - 5432:5432
    healthcheck:
      test: [ "CMD", "pg_isready" ]
      interval: 10s
      timeout: 5s
      retries: 5
  
  db_admin:
    image: dpage/pgadmin4
    container_name: newton-db-admin
    restart: always
    secrets:
      - db-admin-email
      - db-admin-password
    environment:
      - PGADMIN_DEFAULT_EMAIL=/run/secrets/db-admin-email
      - PGADMIN_DEFAULT_PASSWORD_FILE=/run/secrets/db-admin-password
    ports:
      - 8888:80
    volumes:
      - db-admin-data:/var/lib/pgadmin
    depends_on:
      db:
        condition: service_healthy

volumes:
  db-data:
  db-admin-data:

secrets:
  db-name:
    file: env/DB_NAME.txt
  db-password:
    file: env/DB_PWD.txt
  db-admin-email:
    file: env/DB_ADMIN_EMAIL.txt
  db-admin-password:
    file: env/DB_ADMIN_PWD.txt
